# D4 / D5 / D8 / D10 — re-test after fixes

**Run:** 2026-08-20, ~21:33–21:55 IST
**Build:** local dev server, `http://localhost:5173` — **not staging**
**Operator:** admin (user 2)
**Journey used:** **40** — Phase3 Midflow Candidate, cv 292, mrf 137, `PHASE3-TESTPASS-FIXTURE`, sitting on `tech1` with no live booking at start
**Interviewer address:** `pkmondal@aapnainfotech.com` (deliberately *not* `n8npankajmondal@gmail.com`, per the Group 1 warning)

## Verdicts

| # | Defect | Verdict | One-line |
|---|---|---|---|
| **D4** | Reschedule kills the Teams link; neither email carries the new one | 🔴 **STILL OPEN — both halves** | Event is still destroyed and recreated, and both reschedule notices contain **no join link at all** |
| **D5** | Candidate email edits never reach a live journey | 🔴 **STILL OPEN** | Record updated to a new address; journey and send path both still resolve the old one |
| **D8** | Public upload page discards the server's reason | 🔴 **STILL OPEN** | Candidate still sees *"1 of 1 could not be sent. Please try those again."* |
| **D10** | Drawer shows the dead Teams link after a reschedule | ✅ **FIXED** | Drawer updates in place — new time, new meeting ID, new passcode, no reopen needed |

---

## D4 — reschedule kills the Teams join link — 🔴 STILL OPEN

### Half 1 — the Graph event is still replaced, not patched

Two consecutive reschedules on journey 40. Every meeting identifier changes each time:

| | Booking row | Start | Teams meeting ID | Passcode | Join URL fragment |
|---|---|---|---|---|---|
| SCHED-01 (book) | **107** | 24 Aug 09:00 IST | `471138124881060` | `oM9s7Bi2` | `…meeting_NTM1NGY2NzQtYWRiNC00YmY4…` |
| Reschedule #1 | **108** | 25 Aug 09:00 IST | `450766067167142` | `sQ3Mi7nY` | `…meeting_ZTY1NjAwY2EtZmFiYy00NjU2…` |
| Reschedule #2 | **109** | 26 Aug 09:00 IST | `482011056442070` | `Bd2wd9NL` | `…meeting_OWJiZTM0MmUtYTU5Yy00Y2Fi…` |

`graph_event_id`, `teams_join_url`, `online_meeting_id`, `teams_meeting_id` and `teams_passcode` **all differ** between consecutive rows. This is delete-and-recreate, exactly as documented. Anyone holding the previous invite has a dead link.

The reschedule modal now *discloses* the behaviour — a warning banner reads:

> Currently scheduled for 24 Aug, 09:00 am. Picking a new time below cancels this slot and books the new one — both parties are emailed the change.

That is honest, but it is disclosure to the recruiter, not a fix, and the candidate never sees it.

### Half 2 — neither reschedule email carries any join link

Captured **pre-send**, from the modal's own `HTML Code` tab (this is the exact payload dispatched):

**Candidate — "Technical Round 1 rescheduled — Phase3 Test Role (1 opening)"**

```html
<p>Dear Phase3 Midflow Candidate,</p>
<p>Your <strong>Technical Round 1</strong> interview for <strong>Phase3 Test Role (1 opening)</strong> has been <strong>rescheduled</strong>.</p>
<p><strong>Previous time:</strong> <span style="text-decoration:line-through;color:#888;">24 August 2026 at 09:00 am IST</span><br />
  <strong>New time:</strong> 25 August 2026 at 09:00 am IST<br />
  <strong>Duration:</strong> 60 minutes
</p>
<p>Please be available a few minutes early. Reply to this email if the new time does not work for you.</p>
<p>Best regards,<br />AAPNA Infotech Recruitment Team</p>
```

**Interviewer — "Interview rescheduled — Technical Round 1: Phase3 Midflow Candidate"**

```html
<p>Hi,</p>
<p>The <strong>Technical Round 1</strong> interview with <strong>Phase3 Midflow Candidate</strong> (Phase3 Test Role (1 opening)) has been <strong>rescheduled</strong>.</p>
<p><strong>Previous time:</strong> <span style="text-decoration:line-through;color:#888;">24 August 2026 at 09:00 am IST</span><br />
  <strong>New time:</strong> 25 August 2026 at 09:00 am IST<br />
  <strong>Duration:</strong> 60 minutes<br />
  <strong>Candidate email:</strong> claudepankajmondal@gmail.com
</p>
<p>Best regards,<br />AAPNA Infotech Recruitment Team</p>
```

**Neither body contains a join URL, a meeting ID or a passcode** — not the new one, and not even the old one. So the app's own mail cannot repair the link it just broke; the candidate depends entirely on Exchange's replacement invite arriving and being distinguishable from the `Canceled:` notice for the destroyed meeting.

### Not verified this run

The mailbox half. There is no email log endpoint on this build (`/api/email/logs`, `/log`, `/outbox`, `/queue`, `/history` all 404; only `/api/email/templates` exists), and I had no access to the `pkmondal@` or staging test inboxes. **Still owed:** count of messages per side per reschedule, and whether Exchange emits its own `Canceled:` notice. Everything above is from the DB rows and the pre-send payloads.

---

## D5 — candidate email edit never reaches a live journey — 🔴 STILL OPEN

**Do:** change candidate 292's email, then look at what the live journey resolves.

`PATCH /api/candidates/292` → **200**, `email` now `claudepankajmondal+d5probe@gmail.com`.

Immediately after (same second):

| Read | Value |
|---|---|
| Candidate record (`/api/candidates/292`) | `claudepankajmondal+d5probe@gmail.com` — ✅ updated |
| Journey 40 shortlist row (`rpa_shortlisted_candidates.candidate_email`) | `claudepankajmondal@gmail.com` — 🔴 stale |
| Pipeline board card for journey 40 | `claudepankajmondal@gmail.com` — 🔴 stale |
| **Interview panel email preview — the `Candidate email:` line the interviewer reads** | `claudepankajmondal@gmail.com` — 🔴 stale |

The last row is the one that matters: the **send path** still resolves the denormalised copy, so the fix was not "resolve the address at send time". None of the three candidate fixes in the checklist has landed.

Reverted: `PATCH /api/candidates/292` → 200, email back to `claudepankajmondal@gmail.com`.

Side note: the only candidate-update route on this build is `PATCH /api/candidates/:id` (`PUT` and `POST` both 404), and the Search Candidate screen's "View Candidate" modal is read-only — so the edit surface itself is API-only from what I could reach in the UI.

---

## D8 — public upload page discards the server's reason — 🔴 STILL OPEN

**Do:** open the public portal for journey 27 (SAHIL SARMA, Dot Net) at `/documents/{token}`, no login, and submit an executable against a pending checklist item.

Probe file: `d8probe.exe`, 600 bytes, real `MZ` header (`0x4D 0x5A`), MIME `application/x-msdownload`, against *Permanent address proof*.

| Observation | Result |
|---|---|
| Client-side filter | 🔴 **None.** All three `input[type=file]` still carry `accept=""`. The `.exe` was accepted and the item flipped to *"Ready to submit"* — **O3 unchanged** |
| Server verdict | ✅ **HTTP 400** on `POST /api/documents/{token}/upload` — the D6 fix holds (no 500), and the executable was refused |
| **What the candidate sees** | 🔴 **`1 of 1 could not be sent. Please try those again.`** |

That string is byte-identical to the one recorded in the checklist. The server's specific reason is still thrown away, and the advice given ("try those again") still cannot work — retrying the same `.exe` will fail identically.

Page text below the checklist does state *"Accepted formats: PDF, DOC/DOCX, JPG or PNG — up to 10 MB each"*, but it is static copy, not connected to the failure, and the toast does not point at it.

Not captured: the server's exact 400 body string for side-by-side contrast — the JS execution tool's safety classifier went down mid-run. The HTTP status is confirmed from the network log.

---

## D10 — drawer shows the dead Teams link after a reschedule — ✅ FIXED

Tested twice, deliberately **without reopening the drawer**.

| | Drawer before | Drawer after, no reopen | DB |
|---|---|---|---|
| Reschedule #1 | 24 Aug 09:00 · ID `471138124881060` · Passcode `oM9s7Bi2` | **25 Aug 09:00 · ID `450766067167142` · Passcode `sQ3Mi7nY`** | row 108 — matches |
| Reschedule #2 | 25 Aug 09:00 · ID `450766067167142` · Passcode `sQ3Mi7nY` | **26 Aug 09:00 · ID `482011056442070` · Passcode `Bd2wd9NL`** | row 109 — matches |

Run #2 was captured within ~4 seconds of the modal closing, and the panel was already correct — no stale intermediate render was observed at any point. Drawer values were compared programmatically against `/api/pipeline/40` and matched exactly (`drawerMatchesDb: true`).

The interim mitigation ("close and reopen the drawer after any reschedule") is no longer needed.

⚠️ **But read this next to D4.** D10 was only ever the *view* half of the problem. The recruiter now sees a correct, live Join button — while the candidate holds a dead one and gets no replacement link in either app email. Fixing D10 removes the recruiter's cue that anything is wrong, so **D4 is now more dangerous than before, not less.**

---

## State left behind

- Journey 40 has a **live booking**: row 109, Tech 1, 26 Aug 2026 09:00 IST, interviewer `pkmondal@aapnainfotech.com`, meeting `482011056442070`. Cancel it if it is not wanted.
- **Real mail was sent** to `pkmondal@aapnainfotech.com` and the staging test inbox: 1 invite + 2 reschedule notices per side.
- Rows 107 and 108 are cancelled; two Graph events were created and destroyed.
- Candidate 292's email was changed and **reverted** to `claudepankajmondal@gmail.com`.
- Journey 27's document portal: one rejected `.exe` submission (plus one duplicate click). Server refused with 400, so no document row or OneDrive file should exist — worth a one-line DB check before sign-off.

## Suggested checklist edits

- **D4** — reword from "Medium, accepted" to blocking. Add the finding that both notices are link-free, and that D10's fix removes the recruiter's only visual cue.
- **D5** — unchanged; the decision is still owed. Note that the send path reads the denormalised copy, which rules out "it already resolves at send time".
- **D8** — unchanged. Confirms it should be fixed together with DOC-04's O3 gap: `accept=""` is still on all three inputs on the public page.
- **D10** — move to ✅ fixed, verified twice without reopen on dev. Re-confirm on staging after a restart.
